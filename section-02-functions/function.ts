function toAdd(firstNumber:number, secondNumber:number):number {
    return firstNumber + secondNumber;
}

const total = toAdd(1, 2);
const total2 = toAdd(123, 456);
console.log(total);
console.log(total2);
